# Minimal dependencies for K12SimWorld curation, screening, generation and tests.
# Browser/video rendering additionally requires the system tools documented in README.md.
jsonschema>=4.21.0
Pillow>=10.0.0
python-dotenv>=1.0.0
requests>=2.31.0
# OpenAI-compatible providers use the OpenAI SDK; the other current provider
# implementations use requests directly.
openai>=1.0.0
